# Indefinite Integral Canonical Fixture

Input problem:

```text
integral tan(x) / (2 sen(x) + 6) dx
```

Required visual moments:

1. Draw axes on a regular interval that avoids the tangent asymptote.
2. Draw the integrand `f(x)`.
3. Sweep signed area from `x0 = 0` to a moving `x`.
4. Draw the primitive curve generated by that accumulated area.
5. Show the vertical family `F(x)+C`.
6. Introduce `t = tan(x/2)` as the exact algebraic route.
7. Convert the integral to a rational function in `t`.
8. Split into partial fractions.
9. Integrate the logarithmic terms.
10. Substitute `t = tan(x/2)` back.
11. Validate by differentiating the result.

Current rendering contract:

- Use one interactive page with a draggable timeline and a Cartesian plot as the main object.
- Keep formulas in typewriter-style TeX text as side support.
- Animate the graph, signed area, primitive curve, family shifts, cards, and arrows smoothly; avoid abrupt scene jumps.
- The mathematical checks must pass before generating the HTML.
